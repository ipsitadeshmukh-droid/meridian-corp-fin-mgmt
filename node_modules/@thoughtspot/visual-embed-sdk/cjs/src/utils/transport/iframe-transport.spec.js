"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const iframe_transport_1 = require("./iframe-transport");
const logger_1 = require("../logger");
const errors_1 = require("../../errors");
describe('iframe-transport', () => {
    describe('resolveMessageOrigin', () => {
        it('returns the origin (scheme://host[:port]) for a valid host URL', () => {
            expect((0, iframe_transport_1.resolveMessageOrigin)('https://my.thoughtspot.com')).toBe('https://my.thoughtspot.com');
            expect((0, iframe_transport_1.resolveMessageOrigin)('https://host.example.com:8443/some/path')).toBe('https://host.example.com:8443');
        });
        it('returns null when the host cannot be parsed', () => {
            expect((0, iframe_transport_1.resolveMessageOrigin)('not-a-url')).toBeNull();
            expect((0, iframe_transport_1.resolveMessageOrigin)('')).toBeNull();
        });
    });
    describe('isMessageFromIframe', () => {
        const host = 'https://ts.example.com';
        const contentWindow = {};
        const iFrame = { contentWindow };
        it('rejects a message whose source is not the iframe window', () => {
            const event = { source: {}, origin: host };
            expect((0, iframe_transport_1.isMessageFromIframe)(event, iFrame, host)).toBe(false);
        });
        it('accepts a message from the iframe window with a matching origin', () => {
            const event = { source: contentWindow, origin: host };
            expect((0, iframe_transport_1.isMessageFromIframe)(event, iFrame, host)).toBe(true);
        });
        it('rejects (and warns) when the origin does not match the expected host', () => {
            const warnSpy = jest.spyOn(logger_1.logger, 'warn').mockImplementation(() => undefined);
            const event = {
                source: contentWindow,
                origin: 'https://evil.example.com',
            };
            expect((0, iframe_transport_1.isMessageFromIframe)(event, iFrame, host)).toBe(false);
            expect(warnSpy).toHaveBeenCalledTimes(1);
            warnSpy.mockRestore();
        });
        it('fails open (accepts) when the expected origin cannot be resolved', () => {
            const event = {
                source: contentWindow,
                origin: 'https://anything.example.com',
            };
            // 'garbage-host' is unparseable → origin check is skipped.
            expect((0, iframe_transport_1.isMessageFromIframe)(event, iFrame, 'garbage-host')).toBe(true);
        });
        it('accepts when the message origin is empty', () => {
            const event = { source: contentWindow, origin: '' };
            expect((0, iframe_transport_1.isMessageFromIframe)(event, iFrame, host)).toBe(true);
        });
        it('rejects when the iframe is not set', () => {
            const event = { source: contentWindow, origin: host };
            expect((0, iframe_transport_1.isMessageFromIframe)(event, null, host)).toBe(false);
        });
    });
    describe('postMessageToIframe', () => {
        const host = 'https://ts.example.com';
        const message = { type: 'someEvent', data: { a: 1 } };
        it('posts with no transferables when no channel is provided', () => {
            const postMessage = jest.fn();
            const iFrame = { contentWindow: { postMessage } };
            (0, iframe_transport_1.postMessageToIframe)(iFrame, message, host);
            expect(postMessage).toHaveBeenCalledWith(message, host, []);
        });
        it('transfers the channel port2 when a channel is provided', () => {
            const postMessage = jest.fn();
            const iFrame = { contentWindow: { postMessage } };
            const port2 = {};
            const channel = { port2 };
            (0, iframe_transport_1.postMessageToIframe)(iFrame, message, host, channel);
            expect(postMessage).toHaveBeenCalledWith(message, host, [port2]);
        });
        it('is a no-op when the iframe has no contentWindow', () => {
            const iFrame = { contentWindow: null };
            expect(() => (0, iframe_transport_1.postMessageToIframe)(iFrame, message, host)).not.toThrow();
        });
    });
    describe('sendMessageWithResponse', () => {
        const host = 'https://ts.example.com';
        const message = { type: 'trigger', data: {} };
        let port1;
        let port2;
        let postMessage;
        let iFrame;
        let OriginalMessageChannel;
        beforeEach(() => {
            port1 = { onmessage: null, close: jest.fn() };
            port2 = {};
            OriginalMessageChannel = global.MessageChannel;
            global.MessageChannel = jest.fn(() => ({ port1, port2 }));
            postMessage = jest.fn();
            iFrame = { contentWindow: { postMessage } };
        });
        afterEach(() => {
            global.MessageChannel = OriginalMessageChannel;
        });
        it('exposes the default response timeout', () => {
            expect(iframe_transport_1.MESSAGE_RESPONSE_TIMEOUT).toBe(30000);
        });
        it('posts the message with the channel port and resolves with the response', async () => {
            var _a;
            const promise = (0, iframe_transport_1.sendMessageWithResponse)(iFrame, message, host);
            expect(postMessage).toHaveBeenCalledWith(message, host, [port2]);
            // Simulate the app posting a response back on the channel — the
            // handler resolves with the event's `data` payload.
            (_a = port1.onmessage) === null || _a === void 0 ? void 0 : _a.call(port1, { data: { value: 'ok' } });
            await expect(promise).resolves.toEqual({ value: 'ok' });
            expect(port1.close).toHaveBeenCalled();
        });
        it('rejects when the response carries a top-level error', async () => {
            var _a;
            const promise = (0, iframe_transport_1.sendMessageWithResponse)(iFrame, message, host);
            (_a = port1.onmessage) === null || _a === void 0 ? void 0 : _a.call(port1, { data: { error: 'boom' } });
            await expect(promise).rejects.toBe('boom');
        });
        it('rejects when the error is nested under data', async () => {
            var _a;
            const promise = (0, iframe_transport_1.sendMessageWithResponse)(iFrame, message, host);
            (_a = port1.onmessage) === null || _a === void 0 ? void 0 : _a.call(port1, { data: { data: { error: 'nested-boom' } } });
            await expect(promise).rejects.toBe('nested-boom');
        });
        it('resolves with an empty response when no data is returned', async () => {
            var _a;
            const promise = (0, iframe_transport_1.sendMessageWithResponse)(iFrame, message, host);
            (_a = port1.onmessage) === null || _a === void 0 ? void 0 : _a.call(port1, { data: undefined });
            await expect(promise).resolves.toBeUndefined();
        });
        it('resolves with a timeout Error when no response arrives in time', async () => {
            jest.useFakeTimers();
            const promise = (0, iframe_transport_1.sendMessageWithResponse)(iFrame, message, host, 100);
            jest.advanceTimersByTime(100);
            const result = await promise;
            expect(result).toBeInstanceOf(Error);
            expect(result.message).toBe(errors_1.ERROR_MESSAGE.TRIGGER_TIMED_OUT);
            expect(port1.close).toHaveBeenCalled();
            jest.useRealTimers();
        });
    });
});
//# sourceMappingURL=iframe-transport.spec.js.map